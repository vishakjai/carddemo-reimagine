# Frozen Oracles
Layout: `<PROGRAM>/<scenario>/` containing byte-exact captured outputs + `manifest.yaml`
(inputs, environment, checksums, capture date). Oracles are IMMUTABLE after freeze;
changes require a BEL ORACLE-GAP entry. See CLAUDE.md §2 Phase 2.
